diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000..85bd4c9
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,30 @@
+# OpenClaw Virtual Engineering Department — Environment Variables
+# Copy this file to .env and fill in your values.
+# These variables are referenced in openclaw config files (config/*.json5).
+
+# === Anthropic ===
+ANTHROPIC_API_KEY=sk-ant-...
+
+# === Slack ===
+SLACK_BOT_TOKEN=xoxb-...
+SLACK_APP_TOKEN=xapp-...
+
+# === GitHub ===
+GITHUB_APP_ID=123456
+GITHUB_APP_PRIVATE_KEY_PATH=/path/to/private-key.pem
+GITHUB_INSTALLATION_ID=12345678
+
+# === ClickUp ===
+CLICKUP_API_TOKEN=pk_...
+
+# === OpenClaw Gateway ===
+# Used for agent-to-agent communication. Set a strong random password.
+GATEWAY_PASSWORD=change-me-to-a-strong-password
+
+# === Optional: Web Search (Brave API) ===
+# BRAVE_API_KEY=BSA...
+
+# === Optional: S3 Backup Storage ===
+# AWS_ACCESS_KEY_ID=...
+# AWS_SECRET_ACCESS_KEY=...
+# S3_BUCKET=s3://your-bucket/backups
diff --git a/.gitignore b/.gitignore
index bd93e22..6c9ccb3 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,6 +1,7 @@
 # Environment and secrets
 .env
 *.env
+!.env.example
 secrets/
 
 # Backups (don't commit actual backups)
diff --git a/README.md b/README.md
index 191f38b..855245e 100644
--- a/README.md
+++ b/README.md
@@ -1,6 +1,6 @@
 # Virtual Engineering Department
 
-An AI-powered engineering team orchestrated by [Clawdbot](https://github.com/clawdbot/clawdbot). Deploy a fleet of specialized Claude Code agents managed by a PM orchestrator that handles task decomposition, dispatch, quality gates, and deployment automation.
+An AI-powered engineering team orchestrated by [OpenClaw](https://github.com/openclaw/openclaw). Deploy a fleet of specialized Claude Code agents managed by a PM orchestrator that handles task decomposition, dispatch, quality gates, and deployment automation.
 
 ## Architecture
 
@@ -10,7 +10,7 @@ An AI-powered engineering team orchestrated by [Clawdbot](https://github.com/cla
 ├─────────────────────────────────────────────────────────────────┤
 │                                                                 │
 │  ┌─────────────────┐         ┌─────────────┐                   │
-│  │   CLAWDBOT PM   │◄───────►│   ClickUp   │                   │
+│  │   OPENCLAW PM   │◄───────►│   ClickUp   │                   │
 │  │   "Oscar"       │         │   (Kanban)  │                   │
 │  │   Orchestrator  │         └─────────────┘                   │
 │  └────────┬────────┘                                           │
@@ -59,7 +59,7 @@ An AI-powered engineering team orchestrated by [Clawdbot](https://github.com/cla
 | Version Control | GitHub |
 | Task Management | ClickUp |
 | Communication | Slack |
-| AI | Claude (Anthropic) via Clawdbot |
+| AI | Claude (Anthropic) via OpenClaw |
 
 ## Live Documentation
 
@@ -72,31 +72,35 @@ An AI-powered engineering team orchestrated by [Clawdbot](https://github.com/cla
 - [Deployment Plan](docs/DEPLOYMENT.md) — Full infrastructure setup guide
 - [PM System Prompt](docs/PM-SYSTEM-PROMPT.md) — Complete orchestration instructions for Oscar
 - [Agent Personalities](agents/) — Individual agent SOUL.md files
-- [Config Templates](config/) — Clawdbot configuration templates
+- [Config Templates](config/) — OpenClaw configuration templates
+- [Environment Variables](.env.example) — All required secrets and config
 - [Scripts](scripts/) — Setup and automation scripts
 
 ## Quick Start
 
-1. **Provision Infrastructure**
-   ```bash
-   # 6x VPS instances (Hetzner CX32 recommended)
-   # See docs/DEPLOYMENT.md for details
-   ```
+### Option A: Single VPS (recommended for getting started)
+
+Run all agents on one server — simpler to manage and sufficient for most teams:
+
+1. **Provision 1x VPS** (Hetzner CX32 or similar: 4 vCPU, 8GB RAM)
+2. **Run setup:** `./scripts/setup-agent.sh oscar`
+3. **Configure integrations** (ClickUp, GitHub, Slack, Anthropic API key)
+4. **Start:** `systemctl start openclaw`
 
-2. **Setup Each Agent**
+### Option B: Multi-VPS (for parallel execution)
+
+1. **Provision 6x VPS instances** (see [Deployment Guide](docs/DEPLOYMENT.md))
    ```bash
-   ./scripts/setup-agent.sh <agent-name>
+   ./scripts/setup-agent.sh <agent-name>  # Repeat for each agent
    ```
-
-3. **Configure Integrations**
+2. **Configure Integrations**
    - ClickUp API token
    - GitHub App credentials
    - Slack bot tokens
    - Anthropic API key
-
-4. **Deploy**
+3. **Deploy**
    ```bash
-   systemctl start clawdbot
+   systemctl start openclaw
    ```
 
 ## Deployment Flow
diff --git a/agents/oscar/SOUL.md b/agents/oscar/SOUL.md
index 072933d..0110890 100644
--- a/agents/oscar/SOUL.md
+++ b/agents/oscar/SOUL.md
@@ -113,7 +113,7 @@ When assigning work, post a ClickUp comment with:
 ## Prime Directives
 
 1. **Never write code yourself** — you coordinate
-2. **Never merge without human approval**
+2. **Never merge without human approval** — only execute `merge and deploy` when a human explicitly commands it
 3. **Never skip security scans** — Vigil reviews every PR
 4. **Never skip QA review** — Quinn reviews every PR
 5. **When uncertain, ask the human** — don't guess
diff --git a/config/clawdbot-agent.json5 b/config/openclaw-agent.json5
similarity index 70%
rename from config/clawdbot-agent.json5
rename to config/openclaw-agent.json5
index e4821f7..ac81b31 100644
--- a/config/clawdbot-agent.json5
+++ b/config/openclaw-agent.json5
@@ -1,5 +1,5 @@
-// Clawdbot configuration for Specialist Agents (Luna, Marcus, Rex, Vigil, Quinn)
-// Copy to /home/openclaw/.clawdbot/clawdbot.json
+// OpenClaw configuration for Specialist Agents (Luna, Marcus, Rex, Vigil, Quinn)
+// Copy to /home/openclaw/.openclaw/openclaw.json
 {
   agents: {
     defaults: {
@@ -20,7 +20,7 @@
   gateway: {
     port: 18789,
     mode: "local",
-    bind: "0.0.0.0",
+    bind: "127.0.0.1",  // Localhost only — specialist agents don't need external access
     auth: { 
       mode: "password", 
       password: "${GATEWAY_PASSWORD}"  // Set a strong password
diff --git a/config/clawdbot-pm.json5 b/config/openclaw-pm.json5
similarity index 89%
rename from config/clawdbot-pm.json5
rename to config/openclaw-pm.json5
index fbdb155..538ffaa 100644
--- a/config/clawdbot-pm.json5
+++ b/config/openclaw-pm.json5
@@ -1,5 +1,5 @@
-// Clawdbot configuration for PM Orchestrator (Oscar)
-// Copy to /home/openclaw/.clawdbot/clawdbot.json
+// OpenClaw configuration for PM Orchestrator (Oscar)
+// Copy to /home/openclaw/.openclaw/openclaw.json
 {
   agents: {
     defaults: {
diff --git a/docs/DEPLOYMENT.md b/docs/DEPLOYMENT.md
index 60f01e7..5ee7ea0 100644
--- a/docs/DEPLOYMENT.md
+++ b/docs/DEPLOYMENT.md
@@ -10,7 +10,7 @@ Complete infrastructure setup for the Virtual Engineering Department.
 ├─────────────────────────────────────────────────────────────────┤
 │                                                                 │
 │  ┌─────────────────┐         ┌─────────────┐                   │
-│  │   CLAWDBOT PM   │◄───────►│   ClickUp   │                   │
+│  │   OPENCLAW PM   │◄───────►│   ClickUp   │                   │
 │  │   "Oscar"       │         │   (Kanban)  │                   │
 │  │   Orchestrator  │         └─────────────┘                   │
 │  └────────┬────────┘                                           │
@@ -82,7 +82,7 @@ Add to `/etc/hosts` on each VPS:
 
 ### Create a GitHub App
 
-1. Go to: `https://github.com/organizations/YOUR_ORG/settings/apps/new`
+1. Go to: `https://github.com/organizations/<YOUR_GITHUB_ORG>/settings/apps/new`
 2. Name: `OpenClaw Engineering`
 3. Permissions:
    - Repository: Contents (Read & Write)
@@ -111,7 +111,7 @@ Run the setup script on each VPS:
 ssh root@<vps-ip>
 
 # Download and run setup script
-curl -O https://raw.githubusercontent.com/YOUR_ORG/virtual-eng-dept/main/scripts/setup-agent.sh
+curl -O https://raw.githubusercontent.com/<YOUR_GITHUB_ORG>/virtual-eng-dept/main/scripts/setup-agent.sh
 chmod +x setup-agent.sh
 ./setup-agent.sh <agent-name>  # oscar, luna, marcus, rex, vigil, quinn
 ```
@@ -119,7 +119,7 @@ chmod +x setup-agent.sh
 ### Manual Steps After Script
 
 1. Copy the agent's SOUL.md to `/home/openclaw/workspace/`
-2. Copy clawdbot.json to `/home/openclaw/.clawdbot/`
+2. Copy openclaw.json to `/home/openclaw/.openclaw/`
 3. Setup GitHub CLI auth:
    ```bash
    su - openclaw
@@ -127,16 +127,16 @@ chmod +x setup-agent.sh
    ```
 4. Start the service:
    ```bash
-   systemctl start clawdbot
+   systemctl start openclaw
    ```
 
 ---
 
-## Phase 4: Clawdbot Configuration
+## Phase 4: OpenClaw Configuration
 
 ### PM Orchestrator (Oscar)
 
-**`/home/openclaw/.clawdbot/clawdbot.json`:**
+**`/home/openclaw/.openclaw/openclaw.json`:**
 
 ```json5
 {
@@ -186,7 +186,7 @@ Each specialist agent uses a simpler config focused on Claude Code execution:
   gateway: {
     port: 18789,
     mode: "local",
-    bind: "0.0.0.0",
+    bind: "127.0.0.1",  // Localhost only — agents don't need external access
     auth: { mode: "password", password: "${GATEWAY_PASSWORD}" }
   }
 }
@@ -296,7 +296,7 @@ crontab -e
 │                                                                  │
 │  4. HUMAN APPROVAL                                               │
 │     Human tests on staging                                       │
-│     Human: "merge and deploy" (via Clawdbot PM)                 │
+│     Human: "merge and deploy" (via OpenClaw PM)                 │
 │                                                                  │
 │  5. PRODUCTION                                                   │
 │     Oscar triggers merge                                         │
@@ -310,18 +310,7 @@ crontab -e
 
 ## Human Commands
 
-Issue natural language commands to Oscar via Slack or Clawdbot:
-
-| Command | Action |
-|---------|--------|
-| `merge and deploy [task]` | Merges PR, triggers production deploy |
-| `hold [task]` | Pauses work, moves to backlog |
-| `prioritize [task]` | Bumps to top of queue |
-| `assign [task] to [agent]` | Manual dispatch override |
-| `status` | Reports all active tasks |
-| `deploy staging [task]` | Forces staging redeploy |
-| `rollback [task]` | Reverts last production deploy |
-| `what's blocking?` | Lists blocked tasks |
+See [PM System Prompt — Human Commands](PM-SYSTEM-PROMPT.md#human-commands) for the full command reference.
 
 ---
 
@@ -354,10 +343,10 @@ Issue natural language commands to Oscar via Slack or Clawdbot:
 [ ] Per-Agent Setup (repeat 6x)
     [ ] Run setup-agent.sh
     [ ] Copy SOUL.md
-    [ ] Configure clawdbot.json
+    [ ] Configure openclaw.json
     [ ] GitHub CLI auth
     [ ] Test Claude Code headless
-    [ ] Start Clawdbot service
+    [ ] Start OpenClaw service
 
 [ ] Vigil-Specific
     [ ] Setup backup script
diff --git a/docs/PM-SYSTEM-PROMPT.md b/docs/PM-SYSTEM-PROMPT.md
index 1b5796c..0ff05e0 100644
--- a/docs/PM-SYSTEM-PROMPT.md
+++ b/docs/PM-SYSTEM-PROMPT.md
@@ -1,6 +1,6 @@
 # OpenClaw PM Orchestrator — System Prompt
 
-You are **Oscar**, the Project Manager for the Virtual Engineering Department. You run on Clawdbot and coordinate specialized Claude Code agents that execute development work autonomously.
+You are **Oscar**, the Project Manager for the Virtual Engineering Department. You run on OpenClaw and coordinate specialized Claude Code agents that execute development work autonomously.
 
 Your job: receive tasks from ClickUp, decompose them into agent-assignable tickets, dispatch to the right specialist agent, track progress, enforce quality gates, and report results back to ClickUp and the team via Slack.
 
@@ -39,6 +39,8 @@ You (Oscar — PM Orchestrator)
 
 Each agent is a Claude Code instance running on a dedicated VPS with full repo access via `--dangerously-skip-permissions`.
 
+> **Note:** Agent scope restrictions (branch prefixes, file boundaries) are enforced via prompt instructions in each agent's SOUL.md, not at the filesystem level. Quinn's QA review should flag any out-of-scope file changes. For stricter enforcement, consider Git hooks or CI checks that reject PRs touching files outside the agent's assigned paths.
+
 | Agent | Name | Branch Prefix | ClickUp Tag | Specialty |
 |-------|------|--------------|-------------|-----------|
 | Frontend | **Luna** | `feat/fe-*` | `frontend` | Tailwind, vanilla JS, Jinja2 templates, dashboards |
@@ -49,6 +51,8 @@ Each agent is a Claude Code instance running on a dedicated VPS with full repo a
 
 ### Vigil's Dual Role (Security + Backup)
 
+> **Design note:** Vigil combines security scanning and backup management to keep the agent count (and VPS cost) low. If your team scales beyond 5-6 concurrent tasks, consider splitting these into separate agents. Security scans are latency-sensitive (they block PRs); backups run off-hours via cron and rarely conflict.
+
 **Reactive** (triggered by you on every PR before it moves to QA):
 - Scan for hardcoded secrets, API keys, tokens, credentials
 - Check dependencies for known CVEs (`pip audit`)
@@ -233,8 +237,8 @@ Only Critical and High block the PR from moving to QA.
 ## What You Do NOT Do
 
 - **Do not write code.** You are the PM, not a developer.
-- **Do not merge PRs.** The CTO is the only merge authority.
-- **Do not create ClickUp tasks.** You decompose and assign existing ones.
+- **Do not merge PRs on your own.** Only merge when a human explicitly commands `merge and deploy`. The CTO is the sole merge authority — you execute on their instruction.
+- **Do not create top-level ClickUp tasks.** You decompose existing tasks into subtasks and assign those. Creating subtasks under a parent task is expected.
 - **Do not communicate with CEO** unless CTO explicitly asks you to.
 - **Do not send Ops technical details.** Keep asset/context requests non-technical.
 - **Do not estimate timelines longer than 1 day** without checking with CTO.
diff --git a/docs/flowchart-simple.html b/docs/flowchart-simple.html
index d9a3b67..a1f216d 100644
--- a/docs/flowchart-simple.html
+++ b/docs/flowchart-simple.html
@@ -1,249 +1,228 @@
 <!DOCTYPE html>
 <html lang="en">
 <head>
-  <meta charset="UTF-8">
-  <meta name="viewport" content="width=device-width, initial-scale=1.0">
-  <title>How Work Gets Done — Virtual Engineering Pipeline</title>
-  <style>
-    @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=Caveat:wght@500;600&display=swap');
-
-    * { margin: 0; padding: 0; box-sizing: border-box; }
-
-    body {
-      background: #faf9f7;
-      color: #2d2a26;
-      font-family: 'Outfit', sans-serif;
-      min-height: 100vh;
-    }
-
-    .canvas {
-      max-width: 720px;
-      margin: 0 auto;
-      padding: 56px 28px 80px;
-    }
-
-    .header {
-      text-align: center;
-      margin-bottom: 56px;
-    }
-    .header .eyebrow {
-      font-size: 12px;
-      font-weight: 600;
-      letter-spacing: 0.18em;
-      text-transform: uppercase;
-      color: #a09888;
-      margin-bottom: 12px;
-    }
-    .header h1 {
-      font-size: 32px;
-      font-weight: 700;
-      color: #1a1815;
-      line-height: 1.2;
-      margin-bottom: 12px;
-    }
-    .header p {
-      font-size: 15px;
-      color: #7a7268;
-      max-width: 480px;
-      margin: 0 auto;
-      line-height: 1.6;
-    }
-
-    .step {
-      position: relative;
-      display: flex;
-      gap: 24px;
-      align-items: flex-start;
-      padding-bottom: 20px;
-    }
-    .step-left {
-      display: flex;
-      flex-direction: column;
-      align-items: center;
-      flex-shrink: 0;
-      width: 56px;
-    }
-    .step-number {
-      width: 56px;
-      height: 56px;
-      border-radius: 50%;
-      display: flex;
-      align-items: center;
-      justify-content: center;
-      font-size: 20px;
-      font-weight: 600;
-      flex-shrink: 0;
-      position: relative;
-      z-index: 2;
-    }
-    .step-line {
-      width: 3px;
-      flex: 1;
-      min-height: 32px;
-      border-radius: 2px;
-    }
-    .step-content {
-      flex: 1;
-      padding: 12px 0 24px;
-    }
-    .step-who {
-      font-size: 11px;
-      font-weight: 600;
-      letter-spacing: 0.14em;
-      text-transform: uppercase;
-      margin-bottom: 6px;
-    }
-    .step-title {
-      font-size: 20px;
-      font-weight: 600;
-      color: #1a1815;
-      margin-bottom: 8px;
-      line-height: 1.3;
-    }
-    .step-desc {
-      font-size: 14px;
-      color: #6b6358;
-      line-height: 1.7;
-    }
-    .step-desc strong { color: #3d3830; }
-
-    .annotation {
-      font-family: 'Caveat', cursive;
-      font-size: 17px;
-      color: #b8a088;
-      margin-top: 10px;
-      transform: rotate(-1deg);
-      display: inline-block;
-    }
-
-    .theme-blue .step-number { background: #e8f0fe; color: #3b6cb5; }
-    .theme-blue .step-line { background: linear-gradient(180deg, #d4e3fc, #eef4ff); }
-    .theme-blue .step-who { color: #5b8ad4; }
-
-    .theme-green .step-number { background: #e6f5ec; color: #2d8a54; }
-    .theme-green .step-line { background: linear-gradient(180deg, #c8ebd5, #eaf7ef); }
-    .theme-green .step-who { color: #3da366; }
-
-    .theme-purple .step-number { background: #f0ecf9; color: #7453a6; }
-    .theme-purple .step-line { background: linear-gradient(180deg, #ddd4f1, #f2eff9); }
-    .theme-purple .step-who { color: #8e6ebf; }
-
-    .theme-amber .step-number { background: #fef3e2; color: #b07a1a; }
-    .theme-amber .step-line { background: linear-gradient(180deg, #f5e2bf, #fef8ef); }
-    .theme-amber .step-who { color: #c99230; }
-
-    .theme-red .step-number { background: #fce8e8; color: #c0392b; }
-    .theme-red .step-line { background: linear-gradient(180deg, #f5d0d0, #fcf0f0); }
-    .theme-red .step-who { color: #d4524a; }
-
-    .theme-teal .step-number { background: #e4f5f3; color: #1a7a6d; }
-    .theme-teal .step-line { background: linear-gradient(180deg, #c2e8e3, #edf8f6); }
-    .theme-teal .step-who { color: #2a9989; }
-
-    .notif {
-      display: flex;
-      align-items: center;
-      gap: 10px;
-      background: #f3f0ec;
-      border-radius: 10px;
-      padding: 10px 16px;
-      margin-top: 12px;
-      border-left: 3px solid #c9bfb0;
-    }
-    .notif-icon { font-size: 18px; flex-shrink: 0; }
-    .notif-text { font-size: 13px; color: #7a7268; line-height: 1.5; }
-
-    .time-pill {
-      display: inline-block;
-      font-size: 11px;
-      font-weight: 500;
-      letter-spacing: 0.05em;
-      padding: 4px 12px;
-      border-radius: 20px;
-      margin-top: 10px;
-      background: #f0ede8;
-      color: #8a7e70;
-    }
-
-    .section-break {
-      display: flex;
-      align-items: center;
-      gap: 16px;
-      margin: 16px 0 16px 80px;
-    }
-    .section-break::before, .section-break::after {
-      content: '';
-      flex: 1;
-      height: 1px;
-      background: #e5e0da;
-    }
-    .section-break span {
-      font-size: 11px;
-      letter-spacing: 0.15em;
-      text-transform: uppercase;
-      color: #bbb2a4;
-      white-space: nowrap;
-      font-weight: 500;
-    }
-
-    .summary {
-      margin-top: 48px;
-      background: #1a1815;
-      border-radius: 16px;
-      padding: 36px 32px;
-      color: #e8e2da;
-    }
-    .summary h2 {
-      font-size: 18px;
-      font-weight: 600;
-      color: #f5f0ea;
-      margin-bottom: 16px;
-    }
-    .summary-grid {
-      display: grid;
-      grid-template-columns: 1fr 1fr;
-      gap: 20px;
-    }
-    .summary-item {
-      display: flex;
-      gap: 12px;
-      align-items: flex-start;
-    }
-    .summary-icon {
-      width: 36px;
-      height: 36px;
-      border-radius: 10px;
-      display: flex;
-      align-items: center;
-      justify-content: center;
-      font-size: 16px;
-      flex-shrink: 0;
-    }
-    .summary-item h3 {
-      font-size: 13px;
-      font-weight: 600;
-      margin-bottom: 2px;
-    }
-    .summary-item p {
-      font-size: 12px;
-      opacity: 0.6;
-      line-height: 1.5;
-    }
-
-    @media (max-width: 560px) {
-      .summary-grid { grid-template-columns: 1fr; }
-      .step { gap: 16px; }
-      .header h1 { font-size: 26px; }
-    }
-  </style>
+<meta charset="UTF-8">
+<meta name="viewport" content="width=device-width, initial-scale=1.0">
+<title>How It Works — Virtual Engineering Department</title>
+<style>
+  @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=Caveat:wght@500;600&display=swap');
+
+  * { margin: 0; padding: 0; box-sizing: border-box; }
+
+  body {
+    background: #faf9f7;
+    color: #2d2a26;
+    font-family: 'Outfit', sans-serif;
+    min-height: 100vh;
+  }
+
+  .canvas {
+    max-width: 680px;
+    margin: 0 auto;
+    padding: 56px 28px 80px;
+  }
+
+  .header {
+    text-align: center;
+    margin-bottom: 56px;
+  }
+  .header .eyebrow {
+    font-size: 12px;
+    font-weight: 600;
+    letter-spacing: 0.18em;
+    text-transform: uppercase;
+    color: #a09888;
+    margin-bottom: 12px;
+  }
+  .header h1 {
+    font-size: 32px;
+    font-weight: 700;
+    color: #1a1815;
+    line-height: 1.2;
+    margin-bottom: 14px;
+  }
+  .header p {
+    font-size: 15px;
+    color: #7a7268;
+    max-width: 460px;
+    margin: 0 auto;
+    line-height: 1.7;
+  }
+
+  .intro {
+    background: #f3f0ec;
+    border-radius: 14px;
+    padding: 24px 28px;
+    margin-bottom: 48px;
+    line-height: 1.7;
+    font-size: 14px;
+    color: #5a5349;
+  }
+  .intro strong { color: #2d2a26; }
+
+  .step {
+    position: relative;
+    display: flex;
+    gap: 24px;
+    align-items: flex-start;
+    padding-bottom: 16px;
+  }
+  .step-left {
+    display: flex;
+    flex-direction: column;
+    align-items: center;
+    flex-shrink: 0;
+    width: 52px;
+  }
+  .step-number {
+    width: 52px;
+    height: 52px;
+    border-radius: 50%;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    font-size: 19px;
+    font-weight: 600;
+    flex-shrink: 0;
+    z-index: 2;
+  }
+  .step-line {
+    width: 3px;
+    flex: 1;
+    min-height: 28px;
+    border-radius: 2px;
+  }
+  .step-content {
+    flex: 1;
+    padding: 10px 0 24px;
+  }
+  .step-title {
+    font-size: 19px;
+    font-weight: 600;
+    color: #1a1815;
+    margin-bottom: 6px;
+    line-height: 1.3;
+  }
+  .step-desc {
+    font-size: 14px;
+    color: #6b6358;
+    line-height: 1.7;
+  }
+
+  .annotation {
+    font-family: 'Caveat', cursive;
+    font-size: 17px;
+    color: #b8a088;
+    margin-top: 10px;
+    transform: rotate(-1deg);
+    display: inline-block;
+  }
+
+  .theme-blue .step-number { background: #e8f0fe; color: #3b6cb5; }
+  .theme-blue .step-line { background: linear-gradient(180deg, #d4e3fc, #eef4ff); }
+
+  .theme-green .step-number { background: #e6f5ec; color: #2d8a54; }
+  .theme-green .step-line { background: linear-gradient(180deg, #c8ebd5, #eaf7ef); }
+
+  .theme-purple .step-number { background: #f0ecf9; color: #7453a6; }
+  .theme-purple .step-line { background: linear-gradient(180deg, #ddd4f1, #f2eff9); }
+
+  .theme-amber .step-number { background: #fef3e2; color: #b07a1a; }
+  .theme-amber .step-line { background: linear-gradient(180deg, #f5e2bf, #fef8ef); }
+
+  .theme-red .step-number { background: #fce8e8; color: #c0392b; }
+  .theme-red .step-line { background: linear-gradient(180deg, #f5d0d0, #fcf0f0); }
+
+  .theme-teal .step-number { background: #e4f5f3; color: #1a7a6d; }
+  .theme-teal .step-line { background: linear-gradient(180deg, #c2e8e3, #edf8f6); }
+
+  .divider {
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    padding: 4px 0 4px 76px;
+    color: #ccc5ba;
+    font-size: 13px;
+    letter-spacing: 0.12em;
+    text-transform: uppercase;
+    font-weight: 500;
+  }
+  .divider::before, .divider::after {
+    content: '';
+    flex: 1;
+    height: 1px;
+    background: #e5e0da;
+    margin: 0 14px;
+  }
+
+  .summary {
+    margin-top: 48px;
+    background: #1a1815;
+    border-radius: 16px;
+    padding: 32px 28px;
+    color: #e8e2da;
+  }
+  .summary h2 {
+    font-size: 17px;
+    font-weight: 600;
+    color: #f5f0ea;
+    margin-bottom: 18px;
+  }
+  .summary-row {
+    display: flex;
+    gap: 12px;
+    align-items: flex-start;
+    margin-bottom: 16px;
+  }
+  .summary-row:last-child { margin-bottom: 0; }
+  .summary-icon {
+    width: 34px;
+    height: 34px;
+    border-radius: 9px;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+    font-size: 15px;
+    flex-shrink: 0;
+  }
+  .summary-row h3 {
+    font-size: 13px;
+    font-weight: 600;
+    margin-bottom: 2px;
+  }
+  .summary-row p {
+    font-size: 12px;
+    opacity: 0.55;
+    line-height: 1.5;
+  }
+
+  .footer {
+    text-align: center;
+    margin-top: 36px;
+    font-size: 12px;
+    color: #bbb2a4;
+  }
+  .footer a { color: #8a7e70; }
+
+  @media (max-width: 520px) {
+    .step { gap: 16px; }
+    .header h1 { font-size: 26px; }
+    .divider { padding-left: 68px; }
+  }
+</style>
 </head>
 <body>
 
 <div class="canvas">
+
   <div class="header">
-    <div class="eyebrow">Virtual Engineering Pipeline</div>
-    <h1>How a task goes from idea to live</h1>
-    <p>An AI-powered engineering pipeline that handles the heavy lifting. Here's what happens at each step and who's involved.</p>
+    <div class="eyebrow">Virtual Engineering Department</div>
+    <h1>How it works</h1>
+    <p>A team of AI workers builds software for you. Here's the process from start to finish.</p>
+  </div>
+
+  <div class="intro">
+    <strong>The idea is simple:</strong> you describe what you need in a task tracker. An AI manager reads it, assigns it to the right AI worker, and the work gets built, checked for quality, tested on a preview site, and reviewed by a human — all before anything goes live.
   </div>
 
   <!-- STEP 1 -->
@@ -253,12 +232,11 @@
       <div class="step-line"></div>
     </div>
     <div class="step-content">
-      <div class="step-who">Your Team</div>
-      <div class="step-title">Drop a task into ClickUp</div>
+      <div class="step-title">You describe what you need</div>
       <div class="step-desc">
-        Describe what you need built or fixed. Include what "done" looks like — the clearer the description, the better the result. Add any relevant links, screenshots, or assets.
+        Write a task in ClickUp. Say what you want built and what "done" looks like. The clearer the description, the better the result.
       </div>
-      <div class="annotation">Just like assigning work to a teammate &#8599;</div>
+      <div class="annotation">That's it — this is the only thing you do</div>
     </div>
   </div>
 
@@ -269,14 +247,9 @@
       <div class="step-line"></div>
     </div>
     <div class="step-content">
-      <div class="step-who">Oscar — AI Project Manager</div>
-      <div class="step-title">The PM picks it up and plans the work</div>
+      <div class="step-title">An AI manager plans the work</div>
       <div class="step-desc">
-        Oscar, the AI project manager, reads the task, figures out what kind of work it is (design, backend logic, server setup, etc.), and assigns it to the right AI specialist. If the task is big, it splits it into smaller pieces that can be worked on at the same time.
-      </div>
-      <div class="notif">
-        <span class="notif-icon">&#128172;</span>
-        <span class="notif-text">You'll see a Slack message: <em>"Starting [task]: assigned to Luna. Estimated time: 2 hours."</em></span>
+        The AI reads your task and figures out what needs to happen. If it's a big job, it breaks it into smaller pieces. It then assigns each piece to the right specialist.
       </div>
     </div>
   </div>
@@ -288,16 +261,14 @@
       <div class="step-line"></div>
     </div>
     <div class="step-content">
-      <div class="step-who">AI Specialists — Luna, Marcus, Rex</div>
-      <div class="step-title">The work gets done</div>
+      <div class="step-title">AI workers build it</div>
       <div class="step-desc">
-        Specialized AI workers write the code. <strong>Luna</strong> handles the visual side (dashboards, pages), <strong>Marcus</strong> builds the behind-the-scenes logic (APIs, data), and <strong>Rex</strong> manages servers and deployment. They can work in parallel, so multiple things happen at once.
+        Specialized AI workers write the code. Multiple workers can run at the same time on different parts of the job, so things move fast.
       </div>
-      <span class="time-pill">&#9201; Most tasks complete in 1&ndash;4 hours</span>
     </div>
   </div>
 
-  <div class="section-break"><span>Automated quality checks begin</span></div>
+  <div class="divider">automatic checks</div>
 
   <!-- STEP 4 -->
   <div class="step theme-red">
@@ -306,15 +277,10 @@
       <div class="step-line"></div>
     </div>
     <div class="step-content">
-      <div class="step-who">Vigil + Quinn — Security &amp; Quality Review</div>
-      <div class="step-title">Two layers of automated checks</div>
+      <div class="step-title">Security and quality checks run automatically</div>
       <div class="step-desc">
-        Before anyone sees the work, it goes through two automated reviews. <strong>Vigil</strong> (security check) scans for anything sensitive that shouldn't be in the code — passwords, keys, known vulnerabilities. <strong>Quinn</strong> (quality check) makes sure the code actually does what was asked and doesn't break anything else.
-      </div>
-      <div class="step-desc" style="margin-top: 8px;">
-        If issues are found, the work is automatically sent back for fixes — up to two times. If it still can't pass, the Tech Lead gets notified.
+        Before anyone sees the work, it goes through two automated reviews. One checks for security problems. The other checks that the code does what was asked. If something's wrong, it gets sent back to be fixed automatically.
       </div>
-      <div class="annotation">Think of it as two editors proofreading before it reaches you</div>
     </div>
   </div>
 
@@ -325,14 +291,9 @@
       <div class="step-line"></div>
     </div>
     <div class="step-content">
-      <div class="step-who">Rex — Automatic Deployment</div>
-      <div class="step-title">Deployed to a test environment</div>
+      <div class="step-title">It goes to a preview site for testing</div>
       <div class="step-desc">
-        Once the automated checks pass, Rex automatically puts the new feature on a <strong>staging site</strong> — a private copy of the app where you can test things without affecting the real product. The system verifies the site loads correctly and nothing is broken.
-      </div>
-      <div class="notif">
-        <span class="notif-icon">&#128279;</span>
-        <span class="notif-text">You'll see a Slack message with a link to preview the changes on the staging site.</span>
+        The new feature is automatically published to a private preview site where you can see it working without affecting the real product.
       </div>
     </div>
   </div>
@@ -344,63 +305,57 @@
       <div class="step-line"></div>
     </div>
     <div class="step-content">
-      <div class="step-who">Tech Lead (Human)</div>
-      <div class="step-title">Human review and approval</div>
+      <div class="step-title">A human reviews and approves</div>
       <div class="step-desc">
-        The Tech Lead opens the staging link, tests the feature, and reviews the code. <strong>Nothing goes live without human approval.</strong> If changes are needed, it goes back for another round. If it looks good, they tell Oscar to merge and deploy.
+        The tech lead tests the feature on the preview site and reviews the code. Nothing goes live without human approval.
       </div>
     </div>
   </div>
 
-  <!-- STEP 7 -->
+  <!-- DONE -->
   <div class="step theme-green">
     <div class="step-left">
-      <div class="step-number">&#10003;</div>
+      <div class="step-number" style="font-size: 22px;">&#10003;</div>
       <div class="step-line" style="background: transparent;"></div>
     </div>
     <div class="step-content">
-      <div class="step-who">Done</div>
-      <div class="step-title">Live in production</div>
+      <div class="step-title">It's live</div>
       <div class="step-desc">
-        Oscar merges the code, Rex deploys it to production, and it's live. ClickUp automatically marks the task as complete. On to the next one.
+        The feature goes to production. The task is automatically marked complete. On to the next one.
       </div>
     </div>
   </div>
 
   <!-- Summary -->
   <div class="summary">
-    <h2>What this means for your team</h2>
-    <div class="summary-grid">
-      <div class="summary-item">
-        <div class="summary-icon" style="background: #2a3a2a; color: #6ee7b7;">&#128203;</div>
-        <div>
-          <h3>Your only job: write good tickets</h3>
-          <p>Describe what you need clearly in ClickUp. The pipeline handles the rest.</p>
-        </div>
-      </div>
-      <div class="summary-item">
-        <div class="summary-icon" style="background: #2a2a3a; color: #818cf8;">&#128172;</div>
-        <div>
-          <h3>Slack keeps you in the loop</h3>
-          <p>You'll see updates in #engineering as tasks move through each stage.</p>
-        </div>
+    <h2>The short version</h2>
+    <div class="summary-row">
+      <div class="summary-icon" style="background: #2a3a2a; color: #6ee7b7;">&#128203;</div>
+      <div>
+        <h3>You write a ticket, AI does the rest</h3>
+        <p>Describe what you need. The pipeline handles planning, building, testing, and deploying.</p>
       </div>
-      <div class="summary-item">
-        <div class="summary-icon" style="background: #3a2a2a; color: #fca5a5;">&#128274;</div>
-        <div>
-          <h3>Three safety nets before anything goes live</h3>
-          <p>Vigil's security scan, Quinn's quality review, plus human approval on staging.</p>
-        </div>
+    </div>
+    <div class="summary-row">
+      <div class="summary-icon" style="background: #3a2a2a; color: #fca5a5;">&#128274;</div>
+      <div>
+        <h3>Nothing ships without checks</h3>
+        <p>Automatic security scan, automatic quality review, and a human signs off before it goes live.</p>
       </div>
-      <div class="summary-item">
-        <div class="summary-icon" style="background: #3a3a2a; color: #fcd34d;">&#9889;</div>
-        <div>
-          <h3>Faster than traditional development</h3>
-          <p>Most tasks go from ClickUp to staging in hours, not days.</p>
-        </div>
+    </div>
+    <div class="summary-row">
+      <div class="summary-icon" style="background: #3a3a2a; color: #fcd34d;">&#9889;</div>
+      <div>
+        <h3>Hours, not days</h3>
+        <p>Most tasks go from ticket to preview site in 1–4 hours.</p>
       </div>
     </div>
   </div>
+
+  <div class="footer">
+    <a href="flowchart.html">View the technical flowchart</a>
+  </div>
+
 </div>
 
 </body>
diff --git a/docs/index.html b/docs/index.html
new file mode 100644
index 0000000..77aa693
--- /dev/null
+++ b/docs/index.html
@@ -0,0 +1,95 @@
+<!DOCTYPE html>
+<html lang="en">
+<head>
+<meta charset="UTF-8">
+<meta name="viewport" content="width=device-width, initial-scale=1.0">
+<title>Virtual Engineering Department — Pipeline Docs</title>
+<style>
+  @import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap');
+  * { margin: 0; padding: 0; box-sizing: border-box; }
+  body {
+    background: #0a0a0f;
+    color: #e0e0e8;
+    font-family: 'DM Sans', sans-serif;
+    min-height: 100vh;
+    display: flex;
+    align-items: center;
+    justify-content: center;
+  }
+  .container {
+    max-width: 520px;
+    text-align: center;
+    padding: 48px 24px;
+  }
+  h1 {
+    font-size: 26px;
+    font-weight: 300;
+    letter-spacing: 0.06em;
+    margin-bottom: 8px;
+  }
+  h1 span { color: #6ee7b7; font-weight: 600; }
+  .sub {
+    font-family: 'JetBrains Mono', monospace;
+    font-size: 11px;
+    color: #555;
+    letter-spacing: 0.12em;
+    text-transform: uppercase;
+    margin-bottom: 48px;
+  }
+  .links { display: flex; flex-direction: column; gap: 16px; }
+  a {
+    display: block;
+    padding: 20px 24px;
+    border-radius: 12px;
+    text-decoration: none;
+    transition: transform 0.15s ease, border-color 0.15s ease;
+  }
+  a:hover { transform: translateY(-2px); }
+  .link-tech {
+    background: linear-gradient(135deg, #1e1a2e, #251640);
+    border: 1px solid #3a2a5a;
+    color: #e9d5ff;
+  }
+  .link-tech:hover { border-color: #c084fc; }
+  .link-simple {
+    background: linear-gradient(135deg, #1a2e1a, #162e21);
+    border: 1px solid #2a4a2a;
+    color: #a7f3d0;
+  }
+  .link-simple:hover { border-color: #6ee7b7; }
+  .link-title { font-size: 16px; font-weight: 600; margin-bottom: 4px; }
+  .link-desc { font-size: 13px; opacity: 0.6; }
+  .gh {
+    margin-top: 40px;
+    font-size: 13px;
+    color: #555;
+  }
+  .gh a {
+    display: inline;
+    color: #818cf8;
+    background: none;
+    border: none;
+    padding: 0;
+    text-decoration: underline;
+  }
+  .gh a:hover { color: #a5b4fc; transform: none; }
+</style>
+</head>
+<body>
+<div class="container">
+  <h1><span>Virtual</span> Engineering Department</h1>
+  <p class="sub">Pipeline Documentation</p>
+  <div class="links">
+    <a href="flowchart.html" class="link-tech">
+      <div class="link-title">Technical Pipeline Flowchart</div>
+      <div class="link-desc">Detailed task lifecycle — agents, quality gates, staging, deploy</div>
+    </a>
+    <a href="flowchart-simple.html" class="link-simple">
+      <div class="link-title">Non-Technical Overview</div>
+      <div class="link-desc">How a task goes from idea to live — for stakeholders</div>
+    </a>
+  </div>
+  <p class="gh"><a href="https://github.com/robertogalan/virtual-eng-dept">View on GitHub</a></p>
+</div>
+</body>
+</html>
diff --git a/scripts/nightly-backup.sh b/scripts/nightly-backup.sh
index 2da5232..8b14f93 100755
--- a/scripts/nightly-backup.sh
+++ b/scripts/nightly-backup.sh
@@ -64,7 +64,7 @@ done
 # Also backup important config files
 echo "[$(date)] Backing up config files..."
 mkdir -p $STAGING_DIR/config
-cp -r /home/openclaw/.clawdbot $STAGING_DIR/config/ 2>/dev/null || true
+cp -r /home/openclaw/.openclaw $STAGING_DIR/config/ 2>/dev/null || true
 cp /home/openclaw/workspace/*.md $STAGING_DIR/config/ 2>/dev/null || true
 
 # Create compressed archive
@@ -96,7 +96,14 @@ echo "[$(date)] Pruning old backups (keeping $RETENTION_DAYS days)..."
 find $BACKUP_DIR -name "backup-*.tar.gz" -mtime +$RETENTION_DAYS | while read old_backup; do
     # Keep weekly backups (Sundays) indefinitely
     BACKUP_DATE=$(basename $old_backup | sed 's/backup-//' | sed 's/.tar.gz//')
-    DAY_OF_WEEK=$(date -d "$BACKUP_DATE" +%u 2>/dev/null || echo "0")
+    # GNU date uses -d, BSD (macOS) uses -j -f
+    if date -d "$BACKUP_DATE" +%u &>/dev/null; then
+        DAY_OF_WEEK=$(date -d "$BACKUP_DATE" +%u)
+    elif date -j -f "%Y-%m-%d" "$BACKUP_DATE" +%u &>/dev/null; then
+        DAY_OF_WEEK=$(date -j -f "%Y-%m-%d" "$BACKUP_DATE" +%u)
+    else
+        DAY_OF_WEEK="0"
+    fi
     
     if [ "$DAY_OF_WEEK" = "7" ]; then
         echo "  Keeping weekly backup: $old_backup"
diff --git a/scripts/setup-agent.sh b/scripts/setup-agent.sh
index 932c1b1..c6ee402 100755
--- a/scripts/setup-agent.sh
+++ b/scripts/setup-agent.sh
@@ -14,6 +14,9 @@ echo "=== Setting up OpenClaw agent: $AGENT_NAME ==="
 # 1. Create service user
 echo "[1/8] Creating service user..."
 useradd -m -s /bin/bash openclaw 2>/dev/null || echo "User openclaw already exists"
+# WARNING: This grants passwordless root access to the openclaw user.
+# For production, scope sudo to only the commands each agent needs.
+# Example: openclaw ALL=(ALL) NOPASSWD: /usr/bin/systemctl restart openclaw, /usr/bin/pm2 *
 echo "openclaw ALL=(ALL) NOPASSWD:ALL" > /etc/sudoers.d/openclaw
 
 # 2. Install system packages
@@ -34,9 +37,9 @@ fi
 echo "[4/8] Installing PM2..."
 npm install -g pm2
 
-# 5. Install Clawdbot
-echo "[5/8] Installing Clawdbot..."
-npm install -g clawdbot
+# 5. Install OpenClaw
+echo "[5/8] Installing OpenClaw..."
+npm install -g openclaw
 
 # 6. Install Claude Code (for specialist agents, not PM)
 if [ "$AGENT_NAME" != "oscar" ]; then
@@ -61,7 +64,7 @@ echo "[8/8] Setting up workspace..."
 su - openclaw << 'EOF'
 mkdir -p ~/workspace
 mkdir -p ~/logs
-mkdir -p ~/.clawdbot
+mkdir -p ~/.openclaw
 
 # Git config
 git config --global user.name "OpenClaw Agent"
@@ -89,18 +92,18 @@ RUNSCRIPT
 chmod +x ~/workspace/run-agent.sh
 EOF
 
-# Setup Clawdbot as systemd service
+# Setup OpenClaw as systemd service
 echo "Setting up systemd service..."
-cat > /etc/systemd/system/clawdbot.service << EOF
+cat > /etc/systemd/system/openclaw.service << EOF
 [Unit]
-Description=Clawdbot Agent ($AGENT_NAME)
+Description=OpenClaw Agent ($AGENT_NAME)
 After=network.target
 
 [Service]
 Type=simple
 User=openclaw
 WorkingDirectory=/home/openclaw/workspace
-ExecStart=/usr/bin/clawdbot gateway
+ExecStart=/usr/bin/openclaw gateway
 Restart=always
 RestartSec=10
 Environment=NODE_ENV=production
@@ -110,17 +113,17 @@ WantedBy=multi-user.target
 EOF
 
 systemctl daemon-reload
-systemctl enable clawdbot
+systemctl enable openclaw
 
 echo ""
 echo "=== Setup complete for $AGENT_NAME ==="
 echo ""
 echo "Next steps:"
 echo "1. Copy SOUL.md to /home/openclaw/workspace/"
-echo "2. Copy clawdbot.json to /home/openclaw/.clawdbot/"
-echo "3. Set environment variables in /home/openclaw/.clawdbot/clawdbot.json"
+echo "2. Copy openclaw.json to /home/openclaw/.openclaw/"
+echo "3. Set environment variables in /home/openclaw/.openclaw/openclaw.json"
 echo "4. Setup GitHub auth: su - openclaw && gh auth login"
 echo "5. Clone your repos to /home/openclaw/workspace/"
-echo "6. Start service: systemctl start clawdbot"
-echo "7. Check logs: journalctl -u clawdbot -f"
+echo "6. Start service: systemctl start openclaw"
+echo "7. Check logs: journalctl -u openclaw -f"
 echo ""
